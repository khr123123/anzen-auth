spring:
  datasource:
    url: jdbc:mysql://localhost:3306/anzen_auth?useSSL=false&serverTimezone=UTC
    username: root
    password: admin123


# springdoc-openapi????
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: org.khr.anzenauth.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn